trigger:
- master

pool:
  name: agent-pool  # Your self-hosted Linux agent pool

steps:
# Step 1: Detect JDK 11 installation and set JAVA_HOME_11_X64
- script: |
    # Find JDK 11 path
    JDK_PATH=$(dirname $(dirname $(readlink -f $(which javac)_



